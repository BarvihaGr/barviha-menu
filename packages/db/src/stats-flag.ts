/**
 * Единый выключатель счётчиков "просмотры / добавления в корзину" (см.
 * stats.ts) — общий и для apps/menu (пишет события), и для apps/hub
 * (показывает вкладку «Статистика»). Отдельный файл без fs — безопасен для
 * клиентских компонентов ('use client' кнопки «в корзину»), в отличие от
 * stats.ts, который читает/пишет content-store.
 *
 * Включить счётчики: поставить true. Больше никаких правок не требуется —
 * вся остальная инфраструктура (API, запись на item-странице, вкладка в
 * бэк-офисе) уже подключена и просто начнёт получать данные.
 */
export const STATS_ENABLED = false;
